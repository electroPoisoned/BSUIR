package music.styles;

import music.Track;

public class Alternative extends Track {
    public Alternative(String title, String author, int duration) {
        super(title, author, duration);
    }
}
